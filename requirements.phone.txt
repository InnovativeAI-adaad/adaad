# ADAAD — Phone-safe requirements
# Architecture: armv8l / aarch64 (Termux on Android)
#
# WHY THIS EXISTS
# ---------------
# requirements.server.txt includes packages with Rust extensions:
#   pydantic-core  (pydantic v2 backend) → maturin fails on armv8l
#   jiter          (anthropic dependency) → same maturin failure
#
# WHAT CHANGES vs requirements.server.txt
# ----------------------------------------
#   fastapi 0.99.1     — last version that supports pydantic v1 natively
#   pydantic 1.10.26   — pure Python, no Rust, v1 API
#   starlette 0.27.0   — required by fastapi 0.99.1
#   sse-starlette 1.6.5— last version compatible with starlette 0.27.x
#   uvicorn 0.23.2     — pure Python ASGI server
#   httpx 0.27.2       — pure Python HTTP client
#   PyYAML             — C extension with pure-Python fallback
#
# EXCLUDED (Rust compilation required):
#   anthropic          — needs jiter (Rust). AI calls disabled on armv8l.
#                        On arm64 try: pip install anthropic --only-binary :all:
#
# SYSTEM PACKAGE (install via pkg, not pip):
#   PyNaCl             — pkg install python-nacl
#
# INSTALL ORDER MATTERS — run exactly:
#   pip install "fastapi==0.141.1" "pydantic==1.10.26" "starlette==1.6.0" \
#               "sse-starlette==1.6.5" "uvicorn==0.52.4" "httpx==0.28.1" \
#               --break-system-packages

fastapi==0.141.1
pydantic==1.10.26
starlette==1.6.0
sse-starlette==1.6.5
uvicorn==0.52.4
httpx==0.28.1
PyYAML>=6.0.1
anyio>=3.7.1,<5
sniffio>=1.3.1
h11>=0.16.0,<0.17
click>=8.4.1
certifi
idna

# PHONE-LIBCST-01: pure-Python, no Rust, required by constitutional AST subsystem
libcst>=1.1.0,<2.0
