# Release Notes — v9.14.0

**Date:** 2026-03-20
**Phases:** 78 + 79 · Production Signing Infrastructure + Multi-Generation Lineage Graph
**PR IDs:** PR-78-01 · PR-79-01
**Lane/Tier:** governance / constitutional
**Governor:** Dustin L. Reid

---

## Summary

v9.14.0 delivers two foundational capability layers: production-grade signing infrastructure
with hardware-ready signer abstractions, and the multi-generation lineage DAG that makes
compound evolution across epoch boundaries structurally possible.

---

## Phase 78 — Production Signing Infrastructure + Aponi GitHub Feed + Doc Autosync

### M78-01 — Journal Warm-Cache (`JOURNAL-CACHE-0`)

- `security/ledger/journal.py` — `JOURNAL-CACHE-0` warm-path tail cache; `JournalPaths`
  dataclass + full path-resolution infrastructure
- 9 constitutional tests confirming O(n) append performance, tamper detection, and
  cross-instance isolation

### M78-02 — Autonomous Doc Sync (`DOC-SYNC-VERSION-0`)

- `.github/workflows/docs-autosync.yml` — triggers on VERSION/CHANGELOG/ROADMAP push
- `scripts/verify_doc_sync.py` — exits 1 on any version drift; `DOC-SYNC-DETERM-0` guarantee
- `docs/ADAADCHAT_SETUP.md` — GitHub App wiring guide for ADAADchat operators
- `.env.example` — canonical environment variable reference

### M78-03 — Ed25519/HMAC Production Signers (`LEDGER-SIGN-0`)

- `HMACEnvSigner` (ADAAD_LEDGER_HMAC_SECRET) — HMAC-SHA256 signer
- `Ed25519FileSigner` (ADAAD_SIGNING_KEY_PATH) — Ed25519 file signer
- `build_signer_from_env()` — priority factory resolving signer from environment
- 21 tests

### M78-04 — Aponi GitHub Feed Panel

- `data-view="github"` nav tab
- `loadGithubFeed()` async loader with event-type-keyed CSS classes
  (push / pr / ci / slash / install / rejected)
- Governance bridge fallback for offline operation
- 16 tests

**Phase 78 total: 46 new tests**

**Invariants introduced:**
- `JOURNAL-CACHE-0`: warm-path tail cache; append is O(n), tamper detected via hash chain
- `DOC-SYNC-VERSION-0`: doc-autosync workflow enforces version consistency post-merge
- `DOC-SYNC-DETERM-0`: identical inputs → identical verify_doc_sync.py output
- `LEDGER-SIGN-0`: all ledger events signed via production signer pipeline

---

## Phase 79 — Multi-Generation Lineage Graph

- `runtime/evolution/multi_gen_lineage.py` — `GenerationNode` (frozen dataclass, `node_digest`);
  `MultiGenLineageGraph` (DAG):
  - `register_node`, `ancestor_path`, `descendant_set`, `generation_summary`
  - `graph_digest`, `to_dict`, `from_ledger`
- Foundation for Phase 80 compound multi-seed competitive evolution
- 26 tests

**Invariants introduced:**
- `MULTIGEN-0`: every `GenerationNode` ledger-anchored before registration
- `MULTIGEN-ACYC-0`: DAG; cycles are structurally impossible
- `MULTIGEN-DETERM-0`: identical ledger → identical `graph_digest`
- `MULTIGEN-REPLAY-0`: graph fully reconstructable from ledger alone
- `MULTIGEN-ISOLATE-0`: no shared mutable state between graph instances

---

## Thesis

- `docs/thesis/ADAAD_THESIS.md` — 500-line comprehensive technical thesis covering
  architecture, all 23 constitutional invariants, proven capabilities, operator model,
  current live state, and evolution trajectory

---

## Metrics at v9.14.0

| Metric | Value |
|---|---|
| Tests passing | 4,748+ |
| Phases complete | 79 |
| Constitutional invariants | 23 |
| Evidence ledger entries | 12,441+ |
